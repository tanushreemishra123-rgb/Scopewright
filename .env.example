# Optional live AI provider (OpenAI-compatible). Leave unset to use Mock AI mode.
# The app NEVER requires these — mock mode runs the full experience for free.
VITE_AI_BASE_URL=https://api.openai.com/v1
VITE_AI_API_KEY=sk-...
VITE_AI_MODEL=gpt-4o-mini
